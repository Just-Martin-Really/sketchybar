# Editor / tooling
.claude/
.idea/
.vscode/

# Env / secrets
.env
.env.local
.env.*.local

# macOS
.DS_Store

# Zensical build output
site/

# Local theme override. Keeps a private palette off the repo while the
# machine runs it; themes/active.sh picks it up automatically when present.
themes/local.sh
