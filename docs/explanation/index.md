# Explanation

Background on why individual items are built the way they are. Each page names the macOS behaviour that forced the design, so the workaround is not mistaken for accidental complexity.

Read [macOS constraints](macos-constraints.md) before simplifying any plugin. The obvious version has usually been tried.
